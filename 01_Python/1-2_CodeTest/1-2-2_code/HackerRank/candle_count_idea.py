# list의 내부함수를 사용하여 문제를 해결하였음
# 큰값을 max()를 통해 찾고 list의 count()를 통해 가장 큰 값의 개수를 찾음

a = [1, 3, 3, 2]

print(max(a))

# 파이썬 특정 요소의 개수를 세는 방법
# count() 함수를 이용하면 리스트 내 특정 요소의 개수를 셀 수 있다.

print(a.count(3))